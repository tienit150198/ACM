#include <bits/stdc++.h>
#define ll long long int
#define Max 1007
using namespace std;
ll n, sum;
ll a[Max];

bool isSum(ll b[], ll n){
    for(ll i = 0 ; i < n; i++){
        if(b[i] == 1)
            c += a[i];
    }
    return c == sum;
}

void gen(ll a[], ll n){
    for(ll i = 0 ; i < n; i++){
        a[i] = 0;
    }
    ll i = n - 1;
    while(true){
        i = n - 1;
        while(i-- && i >=)
    }
}

int main()
{
    ios::sync_with_stdio(false);
    cin >> n >> sum;
    bool flag = false;
    for(ll i = 0 ; i < n; i++){
        cin >> a[i];
    }

    cout <<"no";
    return 0;
}
