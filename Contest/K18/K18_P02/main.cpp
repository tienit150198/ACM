#include <bits/stdc++.h>
#define ll long long int
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    ll a, b;
    cin >> a >> b;

    printf("%.2f", (float)a*b);

    return 0;
}
