#include <bits/stdc++.h>
#define ll long long int
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    char s;
    cin >> s;
    if(s >= 'a' && s <= 'z')
        cout <<"THUONG";
    else
        cout <<"HOA";

    return 0;
}
