#include <bits/stdc++.h>
#define ll long long int
#define Max 100007
using namespace std;

ll n, k;
ll a[2007];

priority_queue<ll> pq;

int main()
{
    ios::sync_with_stdio(false);
    cin >> n >> k;

    for(ll i = 0 ; i < n; i++)
        cin >> a[i];

    sort(a, a + n);

    ll i = 0;
    ll ans = 0;


    return 0;
}
